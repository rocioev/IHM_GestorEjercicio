<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.IHM_GestorEjercicio"
        tools:targetApi="31">
        <activity
            android:name=".PerfilActivity"
            android:label="@string/btn_perfil"
            android:exported="false" />
        <activity
            android:name=".RutinasActivity"
            android:label="@string/btn_rutinas"
            android:exported="false" />
        <activity
            android:name=".HistorialActivity"
            android:label="@string/btn_historial"
            android:exported="false" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/btn_home"
            android:exported="false" />
        <activity
            android:name=".MainActivity"

            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>